defmodule Anagrams do

  def start(_type, _args) do
    # {:ok, pid} = Anagrams.Dictionary.start_link
    # Enum.map(1..4, &"words/list#{&1}.txt") |> Anagrams.WordLoader.from_files
    # {:ok, pid}
  end
end
